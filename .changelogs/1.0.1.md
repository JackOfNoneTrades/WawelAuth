Security:
* Hardened fallback texture proxy redirects against SSRF to localhost/private network targets.
* Fixed trusted nginx client IP handling for join/hasJoined without accepting spoofed forwarded headers from public clients.
* Added app-level rate limiting for admin login and Yggdrasil auth endpoints.
* Removed admin session cookie auth and tightened forwarded HTTPS detection behind local proxies.
* Bounded public fallback profile and texture proxy caches to limit memory growth from high-cardinality requests.
* Added same-port HTTPS support so admin login can be protected even without a separate reverse proxy port.

Changes:
* Hardened texture resolving and caching to avoid stale skin/cape state and race-condition bugs.
* Improved the public site and admin panel.
* Added 67minecraft publishing support and filled in Modrinth/CurseForge project metadata.
